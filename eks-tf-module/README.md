## Deploy EKS using Terraform modules.

* some resources are provisioned using different methods than the original deployment.
* for Helm provider, make sure AWS cli is installed where terraform is being ran.